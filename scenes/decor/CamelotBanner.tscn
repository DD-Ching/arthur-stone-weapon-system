[gd_scene load_steps=2 format=3]

; A Camelot / Pendragon standard — pole + a gold pennant blazoned with a red dragon. Set
; `faction` for camelot (gold, default), saxon (moss-green) or rebel (black-purple). Pure
; decor, no physics, no image assets.

[ext_resource type="Script" path="res://scripts/decor/CamelotBanner.gd" id="1_camelotbanner"]

[node name="CamelotBanner" type="Node2D"]
script = ExtResource("1_camelotbanner")
