# ----- Godot 4 -----
# Per-project cache, imported assets, and editor state. Regenerated on open.
.godot/

# Mono / C# glue (unused here, but harmless to ignore)
.mono/
data_*/
mono_crash.*.json

# ----- Exports / build artifacts -----
# Builds belong on a GitHub Release or Pages branch, not in source.
/build/
/builds/
/export/
/dist/
*.pck
*.zip
*.wasm
*.exe
*.x86_64
*.apk
*.aab
*.dmg

# ----- Project-local notes (not part of the public game) -----
# The original briefs / prompts; kept locally, not shipped in the public repo.
finishIt.txt
finishit2.txt
finishit3.txt
finishit4.txt

# Throwaway local verification screenshots (underscore-prefixed).
_*.png
_*.png.import

# ----- OS / editor cruft -----
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Playwright MCP scratch (live-demo verification artifacts)
.playwright-mcp/
